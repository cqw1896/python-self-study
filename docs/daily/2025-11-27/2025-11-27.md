**本周目标**：完成 Python 环境、Git、工具链搭建
**今日完成**：

- [ ] **学习要点**：环境、Git、工具链
```bash
# 1️⃣ 安装 pyenv（macOS/Linux），如果使用brew安装，先安装Homebrew
# 1. 先安装必备的依赖
sudo apt update && sudo apt install -y build-essential procps curl file git

# 2. 运行官方的 Linux 安装脚本安装brew（会自动安装到 ~/ .linuxbrew）
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 3. 按照安装过程中提示，把 Homebrew 加到 shell 配置文件中
# 通常会让你运行下面这两行（根据你的 shell 可能是 bash 或 zsh）：
echo 'eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)"' >> ~/.bash
eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)"
brew --version
Homebrew 5.0.3

# 安装pyenv
brew install pyenv

# 查看pyenv支持的python版本
pyenv install --list

# 安装3.13.9版本
pyenv install 3.13.9

# 使用并验证3.13.9版本的python
pyenv global 3.13.9
pyenv versions
  system
* 3.13.9 (set by /home/think/.pyenv/version)


ssh-keygen -t ed25519 -C <useremail>
cat ~/.ssh/id_ed25519.pub
复制全部输出 → 打开 GitHub 网站 → Settings → SSH and GPG keys → New SSH key → 粘贴 → Add SSH key

进入git项目目录，如果目录已经git init 初始化
git remote -v
git remote add origin git@github.com/<username>/python-self-study.git
git remote -v
git pull origin main

目录没有初始化
git clone https://github.com/cqw1896/python-self-study.git
git remote -v
origin  https://github.com/cqw1896/python-self-study.git (fetch)
origin  https://github.com/cqw1896/python-self-study.git (push)

# 2️⃣ 首次提交并推送 
echo "# Python 自学项目" > README.md  
git add README.md  
git commit -m "feat: init repo"  
git push -u origin main

# 3️⃣ 拉取远程仓库到本地  4️⃣  5️⃣
git init
git remote add origin git@github.com/<username>/python-self-study.git  # 如果是新项目目录，先关联
git pull origin main
```

```bash
# poetry环境安装 black flake8 isort pytest pre-commit
cd python-self-study
poetry init --no-interaction
source $(poetry env info --path)/bin/activate
poetry add black flake8 isort pytest pre-commit
pre-commit install  # 初始化 pre‑commit（提交时自动检查）,即在 git commit 时自动执行并通过
```

```bash
# 单元测试框架 `pytest`、`unittest` 基本写法  
cat <<'EOF'>hello.py
def hello(name: str = 'World') -> str:
    '''返回问候语'''
    return f'Hello, {name}!'
if __name__=='__main__':
    print(hello())
EOF

# 编写对应的单元测试
cat <<'EOF'> tests/test_hello.py
import pytest
from hello import hello

def test_hello_default():
    assert hello()=='Hello, World!'
def test_hello_name():
    assert hello('Alice')=='Hello, Alice!'
EOF

pytest -v   # 运行测试
======================================================== test session starts ========================================================
platform linux -- Python 3.13.9, pytest-9.0.1, pluggy-1.6.0 -- /home/mint24/.cache/pypoetry/virtualenvs/python-self-study-yICse08M-py3.13/bin/python
cachedir: .pytest_cache
rootdir: /home/mint24/python-self-study
configfile: pyproject.toml
plugins: cov-7.0.0
collected 2 items

tests/test_hello.py::test_hello_default PASSED                                                                                [ 50%]
tests/test_hello.py::test_hello_name PASSED                                                                                   [100%]

========================================================= 2 passed in 0.03s =========================================================
```



- [ ] **实操步骤**：见上方命令与代码
- [ ] **成果检查**：所有检查项已满足

**遇到的困难**： 

 彻底删除已经关联的远程仓库后，推送失败

```bash
git remote remove origin
```

**解决方案**：

```bash  
# 1. 确认当前分支是 main（大多数情况都是）
git branch
# 如果前面有 `* main` 就说明已经在 main 分支，直接继续； 如果不是，就切换：
git checkout main
# 或者直接把当前分支改名为 main（推荐）：
git branch -M main

# 2.重新添加正确的远程仓库地址
git remote add origin git@github.com:<username>/python-self-study.git
# 如果ssh远程仓库地址之前使用了https方式，需要先更改为ssh方式
# https方式：git remote add origin https://github.com/<username>/python-self-study.git
git remote set-url origin git@github.com:<username>/python-self-study.git

# 3. 执行第一次推送
git push -u origin main
# 避免第一次推送时加 -u
git config --global push.autoSetupRemote true
```

**遇到困难**

```bash
$ git push
To github.com:xxx/python-self-study.git
 ! [rejected]        main -> main (non-fast-forward)
error: failed to push some refs to 'github.com:xxx/python-self-study.git'
hint: Updates were rejected because the tip of your current branch is behind
hint: its remote counterpart. If you want to integrate the remote changes,
hint: use 'git pull' before pushing again.
hint: See the 'Note about fast-forwards' in 'git push --help' for details.
```



你遇到的这个 Git 错误信息说明：

> 你的本地 `main` 分支落后于 GitHub 上的远程 `main` 分支。  
> 也就是说，远程仓库（GitHub）上已经有新的提交（commits），而你的本地没有这些更新。  
> Git 为了防止你意外覆盖别人的（或你自己其他设备上的）更改，拒绝了这次推送（push）。

---

#### ✅ 正确解决方法：先拉取（pull）远程更改，再推送

请按以下步骤操作：

##### 1. 拉取远程仓库的最新更改并合并到本地
```bash
git pull origin main
```

- 这条命令会从 GitHub 拉取最新的提交，并尝试自动合并到本地分支。
- 如果本地没有新增提交，这通常会直接成功（快进合并）。
- 如果本地也有新提交，Git 可能会创建一个 **合并提交（merge commit）**。

##### 2. 推送合并后的结果到 GitHub
```bash
git push origin main
```

---

#### ⚠️ 如果 `git pull` 时出现 **合并冲突（merge conflict）**：

1. Git 会提示哪些文件有冲突。
2. 打开这些文件，查找类似下面的标记：
   ```text
   <<<<<<< HEAD
   你本地的代码
   =======
   远程仓库的代码
   >>>>>>> commit-id...
   ```
3. 手动编辑文件，保留你想要的代码，删掉冲突标记。
4. 保存后，执行：
   ```bash
   git add .                     # 标记冲突已解决
   git commit                    # 完成合并（通常 Git 会自动生成提交信息）
   git push origin main          # 推送到 GitHub
   ```

---

##### 🚫 不推荐的“暴力”方法（仅限个人项目且你知道后果！）

如果你**确定**远程的更改不重要（比如是你在另一台电脑上提交但你不想要了），可以强制覆盖远程仓库：

```bash
git push --force-with-lease origin main
```

> ❗ 注意：这会**永久删除**远程仓库中你本地没有的提交！  
> 如果是团队项目或你有其他设备，**不要用这个命令**！

---

#### ✅ 建议

- 养成习惯：**每次 push 前先 pull**（或者使用 `git pull --rebase`）。
- 如果经常遇到这类问题，可以考虑设置 Git 自动拉取：
  ```bash
  git config --global pull.rebase true
  ```
  这样 `git pull` 会用 rebase 而不是 merge，历史更干净。

---

```bash
$ git status
On branch main
Your branch is up to date with 'origin/main'.

Changes to be committed:
  (use "git restore --staged <file>..." to unstage)
        modified:   .coverage
        modified:   docs/daily/2025-11-27/2025-11-27.md
        modified:   hello.py
        modified:   tests/test_hello.py
```



#### ✅ 正确操作步骤

##### 1. 先获取远程最新信息（不合并）

```bash
git fetch origin
```

##### 2. 再次检查状态

```bash
git status
```

##### 3. 安全地整合远程更改

**推荐使用 rebase（保持线性历史）**：

```bash
git rebase origin/main
```

- 如果没有冲突，你的本地提交会被“重新应用”在远程最新提交之上。
- 如果有冲突，Git 会暂停，让你解决冲突后执行 `git rebase --continue`。

> 💡 为什么用 `rebase` 而不是 `pull`？  
> 如果因为你已经有**待提交的更改（staged changes）**，但还没 `commit`！  
> `git pull` 会尝试 merge，但 merge 需要一个干净的工作区（或至少已提交）。而 `rebase` 在你**尚未 commit** 的情况下其实不会启动（见下文注意）。

---

#### ⚠️ 重要提醒：你还没有 `commit`！

你当前的修改只是 **staged（已暂存）**，但**还没有提交**。  
在拉取远程更改前，你有两个选择：

---

##### ✅ 推荐做法：先提交，再同步

```bash
# 1. 提交你的更改
git commit -m "Update docs, hello.py and tests"

# 2. 获取远程最新提交
git fetch origin

# 3. 用 rebase 整合（保持历史整洁）
git rebase origin/main

# 如果 rebase 成功（无冲突）：
git push origin main

# 如果有冲突：
#   - 手动解决冲突文件
#   - git add <resolved-files>
#   - git rebase --continue
#   - 然后 git push
```

---

##### 🚫 不推荐：强制覆盖（除非你确定远程更新无关紧要）

如果你确定远程的新提交是无用的（比如误操作），可以用：

```bash
git push --force-with-lease origin main
```

但**风险很高**，一般不建议。

---

#### 总结操作流程（最安全）

```bash
# 1. 提交当前更改
git commit -m "Your commit message"

# 2. 拉取远程最新更改并变基
git pull --rebase origin main

# 3. 如果无冲突，直接推送
git push origin main

# 4. 如果有冲突，解决后：
#    git add .
#    git rebase --continue
#    git push origin main
```

这样就能安全地把你的代码推上去，同时保留远程的新提交。

**总结操作流程**

```bash
# 1. 提交当前更改
git commit -m "Your commit message"

# 2. 拉取远程最新更改并变基
git pull --rebase origin main

# 3. 如果无冲突，直接推送
git push origin main

# 4. 如果有冲突，解决后：
#    git add .
#    git rebase --continue
#    git push origin main
```

